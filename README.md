# MySpanishCar v8

This version keeps the cookie banner and consent preferences, but removes the floating “Cookie settings” button after a visitor chooses.

Visitors can still change their choice from the “Cookie settings” link in the footer, which opens the same preferences panel.

## Upload to GitHub Pages

Upload every file and folder in this directory to the root of the GitHub repository, replacing the previous version.
